Carlos Cuns, Wilson Gil

In order to compile and run the code, the user must include a file name as the
second command argument during runtime.
The file should have a source program to be checked for proper syntax. The code will return a set of
lexemes and their types.

